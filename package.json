{
	"name": "gt-logs",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"reset": "wrangler d1 migrations apply gt-logs && wrangler d1 execute gt-logs --command 'DELETE FROM logs'",
		"reset-remote": "wrangler d1 migrations apply gt-logs --remote && wrangler d1 execute gt-logs --command 'DELETE FROM logs' --remote"
	},
	"devDependencies": {
		"wrangler": "^4.37.1"
	}
}
